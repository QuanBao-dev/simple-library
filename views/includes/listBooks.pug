mixin listBooks(books)
  each book in books
    span&attributes(attributes)
      //- h2= book.title
      a(href="/books/"+book.id)
        img(src= book.coverImagePath, alt="Preview", height="150" width="100")
